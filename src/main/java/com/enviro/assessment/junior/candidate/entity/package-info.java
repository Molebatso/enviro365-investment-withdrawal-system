/**
 * JPA entities representing the relational domain model:
 * Investor -> Portfolio -> InvestmentProduct, and Investor -> WithdrawalNotice.
 *
 * Entities are never returned directly from controllers - see the dto
 * package for the objects that actually cross the API boundary.
 */
package com.enviro.assessment.junior.candidate.entity;
